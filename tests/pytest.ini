[pytest]
markers=
	short_test: marker for short (not time-consuming) test
	long_test: marker for long (time-consuming) test
# log_cli = 1 : do not capture log messages (see logs 'live' insteadof at the end with output captured by pytest)
log_cli = 1
addopts = -s
